<%= form_with(model: @user, url: users_path,  local:true) do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <%= f.submit 'Sign in' %>
<% end %>
